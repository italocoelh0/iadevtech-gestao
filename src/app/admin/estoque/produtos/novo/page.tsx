import { prisma } from "@/lib/prisma"; import { PERMISSIONS,requirePermission } from "@/lib/auth/permissions"; import { Card,CardContent,CardHeader,CardTitle } from "@/components/ui/card"; import { ProductForm } from "@/components/stock/product-form"; import { createProduct } from "../../actions";
export default async function Page(){await requirePermission(PERMISSIONS.STOCK_PRODUCTS);const categories=await prisma.productCategory.findMany({where:{active:true},orderBy:{name:"asc"}});return <Card><CardHeader><CardTitle>Novo produto</CardTitle></CardHeader><CardContent><ProductForm action={createProduct} categories={categories}/></CardContent></Card>}
