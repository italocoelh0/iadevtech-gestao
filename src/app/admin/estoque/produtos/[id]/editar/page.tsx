import { notFound } from "next/navigation"; import { prisma } from "@/lib/prisma"; import { PERMISSIONS,requirePermission } from "@/lib/auth/permissions"; import { Card,CardContent,CardHeader,CardTitle } from "@/components/ui/card"; import { ProductForm } from "@/components/stock/product-form"; import { updateProduct } from "../../../actions";
export default async function Page({params}:{params:Promise<{id:string}>}){await requirePermission(PERMISSIONS.STOCK_PRODUCTS);const {id}=await params;const [p,categories]=await Promise.all([prisma.product.findUnique({where:{id}}),prisma.productCategory.findMany({where:{active:true},orderBy:{name:"asc"}})]);if(!p)notFound();return <Card><CardHeader><CardTitle>Editar {p.name}</CardTitle></CardHeader><CardContent><ProductForm action={updateProduct.bind(null,id)} categories={categories} initial={{...p,salePrice:p.salePrice.toString(),costPrice:p.costPrice?.toString(),minimumStock:p.minimumStock.toString()}}/></CardContent></Card>}
