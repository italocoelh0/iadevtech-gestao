import { prisma } from "@/lib/prisma"; import { PERMISSIONS,requirePermission } from "@/lib/auth/permissions"; import { Card,CardContent,CardHeader,CardTitle } from "@/components/ui/card"; import { MovementForm } from "@/components/stock/movement-form";
export default async function Page({searchParams}:{searchParams:Promise<{product?:string}>}){await requirePermission(PERMISSIONS.STOCK_MOVEMENTS);const sp=await searchParams;const [products,events]=await Promise.all([prisma.product.findMany({where:{active:true},select:{id:true,name:true,unit:true},orderBy:{name:"asc"}}),prisma.event.findMany({where:{status:{in:["PUBLISHED","REGISTRATIONS_OPEN","REGISTRATIONS_CLOSED","IN_PROGRESS"]}},select:{id:true,name:true},orderBy:{startsAt:"desc"}})]);return <Card><CardHeader><CardTitle>Nova movimentação de estoque</CardTitle></CardHeader><CardContent><MovementForm products={products} events={events} defaultProductId={sp.product}/></CardContent></Card>}
